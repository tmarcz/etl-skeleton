info:
  title: FTP Source
  description: FTP Source Configuration
  version: 1.0.0

components:
  schemas:
    FtpSourceModel:
      id: FtpSource
      title: FTP Source
      description: FTP Source Configuration
      type: object
      properties:
        user:
          type: string
        password:
          type: string
        path:
          type: string
      required: ["user", "password", "path"]
